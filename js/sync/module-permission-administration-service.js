(function(global){
  'use strict';
  var MODULE_KEYS=Object.freeze(['warehouse','reservations']);
  function outcome(ok,status,data,error){return {ok:ok,status:status,data:data||null,error:error||null};}
  function invalid(){return Promise.resolve(outcome(false,'invalid_input'));}
  function moduleKey(value){value=String(value||'');return MODULE_KEYS.indexOf(value)>=0?value:null;}
  function text(value){return value==null?null:String(value);}
  function uuid(value){value=String(value||'');return /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value)?value:null;}
  function operationId(){return global.crypto&&typeof global.crypto.randomUUID==='function'?global.crypto.randomUUID():null;}
  function errorCode(error){var value=String(error&&error.code||error&&error.message||'MODULE_PERMISSION_ADMINISTRATION_FAILED');return /^[A-Z][A-Z0-9_]{0,95}$/.test(value)?value:'MODULE_PERMISSION_ADMINISTRATION_FAILED';}
  function invoke(operation,args){if(!global.PlatformDeviceSession||typeof global.PlatformDeviceSession.invokeProtected!=='function')return Promise.reject({code:'DEVICE_SESSION_UNAVAILABLE'});if(Object.prototype.hasOwnProperty.call(args||{},'p_actor_device_id')||Object.prototype.hasOwnProperty.call(args||{},'p_device_id'))return Promise.reject({code:'ACTOR_DEVICE_OVERRIDE_DENIED'});return global.PlatformDeviceSession.invokeProtected(operation,args||{});}
  function run(call,normalize,status){return Promise.resolve().then(call).then(function(value){var data=normalize(value);return data==null?outcome(false,'malformed_response'):outcome(true,status,data);}).catch(function(error){return outcome(false,'denied',null,{code:errorCode(error)});});}
  function candidate(row){var id=uuid(row&&row.userId);if(!id)return null;return {userId:id,displayName:text(row.displayName),email:String(row.email||''),accountStatus:String(row.accountStatus||'')};}
  function catalogItem(row){if(!row||!row.permissionKey||String(row.permissionKey).indexOf('module.')===0)return null;return {permissionKey:String(row.permissionKey),displayName:String(row.displayName||row.permissionKey),description:String(row.description||''),allowedScopeMode:String(row.allowedScopeMode||'module'),allowedResourceType:text(row.allowedResourceType),sensitiveMutation:row.sensitiveMutation===true,catalogVersion:Number(row.catalogVersion||0)};}
  function grant(row){var id=uuid(row&&row.grantId);if(!id)return null;return {grantId:id,permissionKey:String(row.permissionKey||''),resourceType:text(row.resourceType),resourceId:text(row.resourceId),grantedAt:text(row.grantedAt),revokedAt:text(row.revokedAt),active:row.revokedAt==null};}
  function resource(row){var id=uuid(row&&row.resourceId),type=text(row&&row.resourceType);if(!id||!type)return null;return {resourceId:id,resourceType:type,code:text(row.code),name:String(row.name||row.displayName||''),displayName:String(row.displayName||row.name||''),status:text(row.status)};}
  function array(value,mapper){if(!Array.isArray(value))return null;var mapped=value.map(mapper);return mapped.some(function(item){return !item;})?null:mapped;}
  function probeAvailability(selectedModuleKey){var selected=moduleKey(selectedModuleKey);if(!selected)return invalid();return run(function(){return invoke('get_user_management_actor_capabilities',{});},function(value){if(!value||value.status!=='success'||typeof value.canManageAccount!=='boolean')return {ownerConfirmed:false};return {ownerConfirmed:value.canManageAccount===true};},'confirmed').then(function(capability){return listCatalog(selected).then(function(response){if(!response.ok)return response;return outcome(true,'available',{moduleKey:selected,catalog:response.data.catalog,ownerConfirmed:capability.ok&&capability.data.ownerConfirmed===true,canManageModuleManagers:capability.ok&&capability.data.ownerConfirmed===true});});});}
  function searchCandidates(selectedModuleKey,query){var selected=moduleKey(selectedModuleKey);if(!selected)return invalid();return run(function(){return invoke('search_module_permission_candidates',{p_module_key:selected,p_query:String(query||'').slice(0,160),p_limit:50});},function(value){var rows=array(value,candidate);return rows&&{candidates:rows};},'listed');}
  function listCatalog(selectedModuleKey){var selected=moduleKey(selectedModuleKey);if(!selected)return invalid();return run(function(){return invoke('list_module_permission_catalog_for_administration',{p_module_key:selected});},function(value){var rows=array(value,catalogItem);return rows&&{catalog:rows};},'listed');}
  function listGrants(selectedModuleKey,targetUserId){var selected=moduleKey(selectedModuleKey),target=uuid(targetUserId);if(!selected||!target)return invalid();return run(function(){return invoke('list_module_permission_grants',{p_module_key:selected,p_target_user_id:target});},function(value){if(!value||value.status!=='success'||uuid(value.targetUserId)!==target)return null;var rows=array(value.grants,grant);return rows&&{targetUserId:target,grants:rows};},'listed');}
  function listResources(selectedModuleKey,resourceType){var selected=moduleKey(selectedModuleKey),type=String(resourceType||'');if(!selected||!type)return invalid();return run(function(){return invoke('list_module_permission_resources_for_administration',{p_module_key:selected,p_resource_type:type});},function(value){var rows=array(value,resource);if(!rows||rows.some(function(item){return item.resourceType!==type;}))return null;return {resources:rows};},'listed');}
  function foundationMutation(selectedModuleKey,input){return mutate(selectedModuleKey,input,true);}
  function catalogMutation(selectedModuleKey,input){return mutate(selectedModuleKey,input,false);}
  function mutate(selectedModuleKey,input,foundation){var selected=moduleKey(selectedModuleKey);input=input||{};var target=uuid(input.targetUserId),grantId=input.action==='revoke'?uuid(input.grantId):null,id=operationId(),permission=String(input.permissionKey||'');if(!selected||!target||!id||['grant','revoke'].indexOf(input.action)<0||foundation&&['module.access','module.manage'].indexOf(permission)<0||!foundation&&permission.indexOf(selected+'.')!==0||input.action==='revoke'&&!grantId)return invalid();var args={p_operation_id:id,p_action:foundation&&input.action==='grant'?'create':input.action,p_target_user_id:target,p_module_key:selected,p_permission_key:permission,p_grant_id:grantId,p_revocation_reason:input.action==='revoke'?String(input.revocationReason||'إلغاء الصلاحية من شاشة إدارة صلاحيات الموديولات').slice(0,300):null};if(!foundation){args.p_resource_type=input.resourceType==null?null:String(input.resourceType);args.p_resource_id=input.resourceId==null?null:String(input.resourceId);}return run(function(){return invoke(foundation?'manage_foundation_module_grant':'manage_catalog_module_grant',args);},function(value){return value&&value.grantId?{result:value}:null;},'applied');}
  global.ModulePermissionAdministrationService=Object.freeze({MODULE_KEYS:MODULE_KEYS,isSupportedModule:function(value){return moduleKey(value)!==null;},probeAvailability:probeAvailability,searchCandidates:searchCandidates,listCatalog:listCatalog,listGrants:listGrants,listResources:listResources,foundationMutation:foundationMutation,catalogMutation:catalogMutation});
})(window);
