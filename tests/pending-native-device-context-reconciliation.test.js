'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm'),test=require('node:test');
const integrationSource=fs.readFileSync('js/platform-integration.js','utf8'),gateSource=fs.readFileSync('js/sync/startup-access-gate.js','utf8');
const DEVICE='11ebe6fe-67a3-488c-8128-d15ad2b79140',BINDING='64cf766b-2642-414f-ad38-6e0cf01cb958',THUMB='ddc33db37e1481014b3970e63b282da74743e25d0197b6b4a6e043d03b7ea016';
function harness(options){options=options||{};const ids=['startupAccessGate','applicationTopbar','applicationBody','startupScreen','globalConferenceHeader','device_authorization_administration_root','tab0','tab1','tab2','tab3','tab4','tab5','tab6'],nodes=Object.fromEntries(ids.map(id=>[id,{style:{display:'none'},innerHTML:''}]));let status=options.status||'pending',enrollmentCalls=0,sessionCalls=0;const window={location:{hostname:'ramyawny37.github.io'},history:{pushState(){}},document:{visibilityState:'visible',getElementById:id=>nodes[id]||null,addEventListener(){},querySelector(){return null;}},addEventListener(){},setTimeout(){return 1;},clearTimeout(){},navigator:{platform:'MacIntel'},SupabaseAuth:{initialize:()=>Promise.resolve(),getState:()=>({authenticated:true}),getAccountIdentity:()=>({authenticated:true}),getSession:()=>({user:{id:'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa'}})},SupabaseClientLayer:{getClient:()=>({auth:{onAuthStateChange:()=>({data:{subscription:{}}})}})},SupabaseDeviceIdentity:{getCurrent:()=>({id:DEVICE,platform:'MacIntel'}),getOrCreate:()=>({id:DEVICE,platform:'MacIntel'})},FirstSystemBootstrapService:{getStatus:()=>Promise.resolve({ok:true,status:'completed'})},SystemAccessService:{initialize:()=>Promise.resolve(),refresh:()=>Promise.resolve(),getState:()=>({accountStatus:'approved',fresh:true})},CurrentDeviceAuthorizationUI:{getState:()=>({status:'unavailable'})},CurrentDeviceAuthorizationService:{getLastDiagnostic:()=>({})},AccessDiagnosticsUI:{render:()=>''},SyncSettingsUI:{signOut(){}},PlatformDeviceSession:{ensureValid(){sessionCalls++;return Promise.resolve({verified:true});}}};window.window=window;vm.runInNewContext(integrationSource,{window,Promise,Date,String,Array,Object,JSON,Error});window.PlatformDeviceEnrollment={ensure(){enrollmentCalls++;if(options.failure)return Promise.reject({code:'DEVICE_ENROLLMENT_DENIED'});const data={deviceId:DEVICE,bindingId:BINDING,publicKeyThumbprint:THUMB,status},result={record:{deviceId:DEVICE,bindingId:BINDING,publicKeyThumbprint:THUMB},status,data};window.PlatformIntegration.reconcileNativeDevice(result);return Promise.resolve(result);}};vm.runInNewContext(gateSource,{window,Promise,Date,String,Array,Object,setTimeout:window.setTimeout});return {window,nodes,setStatus:value=>{status=value;},counts:()=>({enrollmentCalls,sessionCalls})};}
test('existing pending native device keeps its identity across polls and establishes a session only after approval',async()=>{const state=harness();const startup=()=>state.window.StartupAccessGate.run?state.window.StartupAccessGate.evaluate():null;assert.equal((await state.window.StartupAccessGate.run({completeApplicationStartup:()=>{state.nodes.applicationBody.style.display='block';}})).status,'device');assert.equal(state.window.PlatformIntegration.getContext().deviceId,DEVICE);assert.equal(state.window.PlatformIntegration.getContext().deviceStatus,'pending');assert.match(state.nodes.startupAccessGate.innerHTML,/الحالة:<\/strong> بانتظار الاعتماد/);assert.deepEqual(state.counts(),{enrollmentCalls:1,sessionCalls:0});await startup();assert.equal(state.window.PlatformIntegration.getDeviceIdentity().id,DEVICE);assert.deepEqual(state.counts(),{enrollmentCalls:2,sessionCalls:0});state.setStatus('approved');await startup();assert.deepEqual(state.counts(),{enrollmentCalls:3,sessionCalls:1});assert.equal(state.window.StartupAccessGate.isAllowed(),true);});
test('genuine enrollment failure remains distinct from pending',async()=>{const state=harness({failure:true}),result=await state.window.StartupAccessGate.run({completeApplicationStartup:()=>{}});assert.equal(result.status,'device_error');assert.match(state.nodes.startupAccessGate.innerHTML,/تعذر إنشاء طلب الاعتماد/);assert.deepEqual(state.counts(),{enrollmentCalls:1,sessionCalls:0});});
for(const denied of ['revoked','blocked'])test(denied+' remains fail-closed',async()=>{const state=harness({status:denied}),result=await state.window.StartupAccessGate.run({completeApplicationStartup:()=>{}});assert.equal(result.status,'device');assert.equal(state.window.StartupAccessGate.getState().canonicalState,'DEVICE_REVOKED');assert.equal(state.counts().sessionCalls,0);});
